# Task: CUI-303 Refactor Config Format High-Level API

**Status:** ✅ Complete

**Milestone:** Advanced Serialization (YAML/TOML)

## Description

Refactor the config strategies (json, yaml, toml)

Use the format enum as entrypoint, add a method `get_config_cls` to get proper config class using enum values.

Then modify the test format to include those changes

## Implementation

### Plan (executed)

1. **`src/configui/config/formats.py`** — Added `_CONFIG_CLASSES` dict mapping each `SupportedConfigFormat` enum value to its config class (`JsonConfig`, `YamlConfig`, `TomlConfig`), and a `get_config_cls()` instance method that returns the class via dict lookup.

2. **`tests/test_config/test_formats.py`** — Added parametrized test `test_get_config_cls_returns_correct_class` verifying each format returns the correct class.

### Verification

- `hatch fmt` — clean
- `hatch test` — 55 passed
- `hatch run typing` — no issues